# telemetry-data-converter

This project converts two different telemetry JSON formats into a unified format.

## Features
- JSON parsing
- ISO 8601 to Unix epoch milliseconds conversion
- Location string parsing
- Unit testing in Python

## Run
python main.py
